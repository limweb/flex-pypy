Here are a lot of static (a.k.a. generated by hand by someone) stuff that
the examples need to work.

Normally these files and directories (or part of them) will be copied to 
each example directory at flexcompile time (see the py2flex.sh file in 
each example directory).
